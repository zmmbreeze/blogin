extends layout

append content
	- if (items.length)
		div.list
			h2 Articles
			ul
				- items.forEach(function(item) {
					li
						a(href="#{item.url}", title="#{item.title}") #{item.title}
						time #{item.time}
				- })